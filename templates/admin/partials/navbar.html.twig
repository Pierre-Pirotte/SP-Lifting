<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom border-warning">
    <div class="container-fluid">
        <a class="navbar-brand text-warning" href="{{ path('app_admin') }}">SP-Lifting Administration</a>

        <div class="d-flex align-items-center">
            {% if app.user %}
            <span class="text-white me-3">Connecté : {{ app.user.displayname }}</span>

            <a class="btn btn-primary me-2" href="{{ path('app_home') }}">Retour au site</a>

            <form action="{{ path('app_logout') }}" method="post" class="d-inline">
                <button type="submit" class="btn btn-danger">Déconnexion</button>
            </form>
            {% endif %}
        </div>
    </div>
</nav>