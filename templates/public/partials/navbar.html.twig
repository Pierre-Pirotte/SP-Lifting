<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">SP-Lifting</a>
        <div class="d-flex align-items-center">
            {% if app.user %}
                <span class="text-white me-3">Connecté : {{ app.user.displayname }}</span>

                {% if is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-primary me-2" href="{{ path('app_admin') }}">Administration</a>
                {% endif %}

                <form action="{{ path('app_logout') }}" method="post" class="d-inline">
                    <button type="submit" class="btn btn-danger">Déconnexion</button>
                </form>
            {% else %}
                <a class="btn btn-primary" href="{{ path('app_login') }}">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>